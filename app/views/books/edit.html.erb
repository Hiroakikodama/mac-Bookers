<h1>Editing Book</h1>
<%= form_for(@book) do |f| %>

<p>Title</p>
<%= f.text_field :title %>

<p>Body</p>
<%= f.text_area :body %>

<%= f.submit 'Update Book' %>

<%= link_to 'show', book_path(params[:id])%>
<p>|</p>
<%= link_to 'Back', books_path %>

<% end %>